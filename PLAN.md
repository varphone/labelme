# LabelMe 激光条纹中心线与线宽标注功能改造需求

## 0. 项目定位与长期设计原则

这是一个需要长期维护、持续演进并与现有 LabelMe 机制协同工作的功能，而不是一次性的界面实验。由于该功能尚未正式发布，当前设计不承担旧版 `line_profile` 格式兼容义务；实现可以直接采用最终数据模型。所有实现必须遵循以下原则：

- **复用优先**：优先复用现有 `linestrip` 编辑、圆形半径编辑、Shape 撤销栈、Settings、状态栏、Annotation File 读写和后台任务模式；
- **数据与视图分离**：圆形宽度手柄、边界曲线和采样点是视图层对象，不得伪装成普通 Annotation Shape；
- **Qt-free 核心**：profile 数据模型、校验、插值、弧长映射和测量算法不得依赖 PySide6，便于单元测试、批量处理和未来复用；
- **单一数据归属**：`line_profile` 的字段定义、默认值、校验和 JSON 映射必须有唯一 owner，不能分别在 codec、App 和 Widget 中重复编码；
- **边界清晰**：没有 profile 的普通 `linestrip` 必须继续工作；本功能未发布前，不为尚未稳定的旧 `line_profile` 结构保留兼容层；
- **失败保持原状**：非法输入、保存失败、异步任务过期或 profile 变换失败时，保留最后一个有效 Annotation，不产生部分更新；
- **可解释结果**：自动结果必须携带算法版本、来源、可信度和确认状态，不能把自动推断伪装成人工事实；
- **可测试和可观测**：核心几何、数据校验、保存恢复、异步取消和 UI 关键路径都必须有自动化测试与日志；
- **渐进发布**：首期只发布单张 Image 能力，批量、连续帧、导出和审核系统必须经过独立阶段验收后再扩大范围。

术语遵循项目领域文档：单个对象称为 `Shape`，完整数据称为 `Annotation`，磁盘上的 JSON 称为 `Annotation File`。`line_profile` 是 `linestrip Shape` 的可选扩展，不是新的 Shape 类型。

## 1. 目标与范围

当前使用 `linestrip` 标注激光条纹中心线，但同一条条纹的宽度可能沿线发生明显变化。固定线宽无法表达窄处、宽处、暗区和局部空洞。

本需求增加面向激光条纹的中心线 profile 标注能力：

- 继续使用现有 `linestrip` 表示条纹中心线，不新增 `shape_type`；
- 通过少量共享 profile 锚点表达沿中心线变化的完整宽度和可见程度；
- 记录各位置的可见程度、自动测量来源和可信度；
- 支持自动测量与人工修正，并保证人工结果优先；
- 提供边界预览、检查、撤销和重做；
- 保持没有 profile 的普通 Annotation File 可正常打开和编辑；
- 不要求标注员逐像素描绘条纹左右边界。

首期只覆盖单张 Image 的数据结构、中心线编辑、宽度/可见程度编辑和自动测量。目录批量处理、连续图像/视频帧支持和大规模审核工作流列为后续阶段。

## 2. 标注语义

每条带 profile 的 `linestrip` 必须明确记录路径语义，不能只依赖界面当前状态。

### 2.1 连续中心线模式

`path_mode: "continuous"`

- 中心线表示条纹的连续路径；
- 局部变暗、散斑或小范围空洞不自动删除中心线；
- `visibility=0` 只表示当前位置缺少图像证据，不删除中心线；
- 宽度表示该位置的实际条纹宽度。

### 2.2 仅可见区域模式

`path_mode: "visible_only"`

- 中心线只表示当前确认可见的区域；
- 中间存在真实中断时，必须拆分为多个 `linestrip` Shape，不能用一条带空洞的 linestrip 隐式表达断线；
- 每个拆分后的 Shape 独立保存自己的 `line_profile`；
- 界面必须持续显示当前模式，切换模式必须是明确的用户操作。

默认模式为 `continuous`。

## 3. 用户操作流程

### 3.1 新建中心线

1. 选择线条类别；
2. 使用 `linestrip` 沿条纹中心绘制；
3. 双击或现有快捷键结束绘制；
4. 工具生成临时自动宽度/可见程度测量结果；
5. 显示左右边界、宽度、可见程度和低可信度位置；
6. 标注员插入、删除或修改共享锚点，以及锚点上的宽度和可见程度属性；
7. 自动结果只有在接受或人工确认后才写入 Annotation File；
8. 保存 JSON。

自动结果在接受前不得覆盖人工结果，也不得因为普通打开或重新测量而静默写入原始 Annotation File。

### 3.2 编辑已有中心线

- 没有 `line_profile` 的旧 `linestrip` 必须正常打开；
- 旧 Shape 默认只显示中心线；
- 用户主动执行自动测量后，结果先作为未确认的临时状态存在；
- 用户可以插入、删除、拖动共享 profile 锚点，并分别编辑其宽度或可见程度属性；
- 保存后重新打开，中心线、profile、来源、可信度和审核状态必须保持一致；
- 非法或不完整的 profile 不得导致整个原始 Annotation File 丢失，必须提供明确错误/警告并保留原始数据。

### 3.3 与现有功能的关系

- 现有 linestrip 的创建、选择、移动、顶点编辑、加点、删点、复制和保存行为继续可用；
- 现有 `linestrip` 的 `shape_type` 不改变；
- profile 是 Shape 级扩展，不影响 polygon、rectangle、circle、mask 等其他 Shape 类型；
- 现有 `canvas.allow_out_of_bounds_points` 行为继续生效，不能通过本功能引入全局强制裁剪；
- 现有 `keep_prev`、复制标注和连续文件导航可以复用，但不能在未明确确认时覆盖其他 Image 的 profile。

### 3.4 现有机制复用清单

- 中心线顶点插入、删除、拖动、移动、拆分和合并复用现有 Canvas/Shape 操作，只增加 profile 变换；
- 共享 profile 锚点使用圆形手柄，复用现有 circle 的中心点、半径方向、拖动和数值输入交互；
- 圆形手柄的直径表示完整宽度，内部半径统一换算为 `width / 2`；
- profile 修改复用现有 Shape backup/restore 撤销栈和 `mark_dirty` 自动保存流程；
- 全局测量参数复用 Settings Dialog 和配置 Schema，Shape 级覆盖值写入 `line_profile`；
- 上一帧复制复用现有 Shape 复制和 `copy_annotations_to_next` 保存路径，再按当前 Image 重新测量；
- 自动测量后台任务复用现有 `QThread`/进度对话框模式，但必须使用独立 Worker，不得与模型下载线程共享业务状态；
- 预览复用 Canvas 的缩放、平移、图像坐标转换、颜色主题和状态栏提示；
- profile-aware 边界预览不得复用固定线宽的普通 `shape_to_mask` 结果。
- 宽度和可见程度必须在同一套共享锚点上维护，禁止通过两套独立位置列表建立配对关系。

共享锚点手柄不创建普通 `circle` 或 `point` Shape，不进入 Annotation List，不参与普通圆形填充、导出和栅格化逻辑。

## 4. 中心线编辑与 profile 变换

中心线编辑不能静默丢失或错误重定位 profile。所有变换都必须定义锚点的新位置。

### 4.1 支持的编辑

- 插入、删除和拖动中心线顶点；
- 移动整条中心线；
- 延长和截断中心线；
- 反转中心线方向；
- 合并两条相邻中心线；
- 拆分一条中心线；
- 按固定距离重新采样；
- 根据当前 Setting 将坐标限制在图像范围内。

### 4.2 锚点位置规则

- 锚点位置按中心线的归一化弧长保存，范围为 `[0, 1]`；
- 锚点不绑定固定的中心线顶点编号；
- 中心线发生几何变化后，锚点按定义的弧长映射规则重新定位；
- 反转中心线后，位置 `p` 必须变为 `1-p`，并保持宽度、可见程度和来源信息；
- 插入位于现有线段上的顶点不得改变锚点的物理位置；
- 删除或截断使锚点落在线段外时，必须按产品规则裁剪、映射到端点或提示用户，不能静默丢弃；
- 拆分时，原 profile 必须按左右子线的弧长范围分配并重新归一化；
- 合并时，必须明确两条 profile 的拼接顺序、重叠区处理和冲突解决规则；
- 重新采样只能改变中心线表达，不得改变 profile 的物理位置和数值。

如果某个变换无法可靠映射 profile，操作必须失败并保持原 Annotation，不得生成中心线与 profile 不一致的结果。

## 5. 共享 profile 锚点

### 5.1 数据语义

共享 profile 锚点表示中心线某一归一化弧长位置的一组局部观测。位置只保存一次，宽度和可见程度是该位置上的两个独立、可选属性。

建议的核心类型为：

```python
ProfileAnchor(
    position: float,
    width: ProfileValue[float] | None,
    visibility: ProfileValue[float] | None,
)
```

每个锚点至少包含一个有效属性；允许只有宽度或只有可见程度的锚点，以支持两种指标不同的采样密度。`source`、`confidence` 和 `confirmed` 属于各自的属性，不能放在共享锚点级别。

宽度属性表示该位置的完整条纹宽度，单位为原始 Image 像素。

建议默认生成：

- 中心线起点和终点；
- 自动测量发现明显变化的位置；
- 亮度或可见程度显著变化的位置；
- 自动测量可信度较低的位置。

### 5.2 编辑方式

支持：

1. 拖动左右边界，直接改变宽度；
2. 拖动中心线上的圆形宽度手柄；
3. 在属性面板中输入锚点位置、宽度和可见程度；
4. 插入、删除共享锚点；
5. 在共享锚点上分别确认宽度或可见程度属性。

圆形宽度手柄复用现有 `circle` 的中心点、半径方向、拖动和数值输入交互：

- 手柄中心位于中心线对应的归一化弧长位置；
- 手柄直径表示完整宽度，内部半径换算为 `width / 2`；
- 拖动手柄中心改变锚点位置，拖动手柄边缘改变宽度；
- 手柄显示尺寸应考虑缩放，避免实际宽度较大时遮挡中心线；
- 属性面板字段使用“宽度”，不能把半径直接暴露为宽度，避免单位和含义混淆。

共享锚点手柄只是 profile 的编辑视图，不是独立的 Annotation Shape：

- 不创建普通 `circle` 或 `point` Shape；
- 不进入 Annotation List；
- 不使用独立的 Label、Group 或 Shape Flag；
- 不参与现有 circle 的填充、导出和栅格化逻辑；
- 只将共享位置及其属性写入当前 `linestrip` 的 `line_profile.anchors`。

实现时应复用现有 circle 半径编辑的几何计算、数值提交和撤销模式，但将选中对象改为当前 `linestrip` 的活动共享锚点。手柄边缘默认编辑宽度；可见程度通过属性面板、快捷操作或独立的视觉状态控制，不创建第二套几何锚点。

属性面板至少显示：

- 当前宽度；
- 锚点在中心线上的归一化弧长位置；
- 当前宽度的来源、可信度和确认状态；
- 当前可见程度及其来源、可信度和确认状态；
- 当前锚点是否同时包含宽度和可见程度。

### 5.3 属性操作规则

- 插入锚点时可以只创建宽度属性、只创建可见程度属性，或同时创建两者；
- 若目标位置已存在共享锚点，新增属性必须合并到该锚点，不得生成第二个同位置锚点；
- 删除某个属性只清除该属性；删除共享锚点才同时删除该位置的全部属性；
- 移动共享锚点会同时改变该位置的宽度和可见程度属性位置；
- 自动测量在同一采样位置同时产生宽度和可见程度结果，但允许某个属性因测量失败而为空；
- 属性面板必须明确当前操作作用于“宽度”还是“可见程度”，避免误修改另一属性；
- 清除某一属性后，如果锚点不再包含任何属性，则自动删除该共享锚点。

### 5.4 宽度曲线

- 锚点之间使用明确、可复现的插值方法；首期默认使用分段线性插值；
- 宽度必须是有限正数，不允许零或负数；
- 支持每个 Shape 设置最小、最大测量约束；
- 实际统计的最小、最大、平均宽度不能与约束字段混淆；
- 平滑不得覆盖人工确认的锚点；
- 真实宽度突变必须保留，不得强行平滑；
- 重复位置的锚点必须拒绝或按明确规则合并，不能依赖排序结果。

## 6. 可见程度

可见程度是当前位置在当前 Image 中的可辨识程度，不等同于中心线是否存在。

取值范围为 `[0, 1]`：

- `1.0`：清晰可见；
- `0.5`：较暗、散斑明显或部分空洞；
- `0.0`：确认不可见或没有可靠图像证据。

界面提供：

- 可见程度曲线；
- 清晰、模糊、不可见快捷操作；
- 自动测量结果；
- 人工覆盖自动结果；
- 低可信度位置提示。

可见程度属性必须保存自己的来源、可信度和确认状态。默认 `continuous` 模式下将可见程度设为 0 不得自动删除中心线。可见程度与宽度可以存在于同一共享锚点，也可以在不同位置独立存在。

## 7. 自动宽度测量

### 7.1 测量方式

沿中心线按采样间距取样，在每个采样位置建立局部法线测量线，分析两侧图像并得到宽度候选值。

测量应综合：

- 局部亮度；
- 周围背景亮度；
- 亮度变化位置；
- 连续亮区长度；
- 中心线两侧对称性；
- 局部图像边界和可用采样范围。

不能使用单一的全图固定亮度阈值。

局部法线、急转弯、端点和图像边缘的处理规则必须明确并有单元测试。

### 7.2 异常处理

自动测量必须识别：

- 局部过曝；
- 低对比度；
- 散斑造成的孤立亮点；
- 左右边界不对称；
- 图像边缘越界；
- 测量范围内没有可靠边界。

异常位置必须保存低可信度状态和推荐值。推荐值可以来自邻近可靠位置的插值，但必须允许人工修改和确认。

### 7.3 测量参数

全局默认 Setting 提供：

- 测量方向/法线策略；
- 测量范围；
- 最小宽度；
- 最大宽度；
- 采样间距；
- 平滑程度；
- 亮度和对比度权重。

单个 Shape 可以保存覆盖值。全局默认值属于 Settings，Shape 覆盖值属于 `line_profile`；两者不能混淆。

自动测量必须在后台执行，支持进度、取消和过期结果丢弃。若测量期间中心线或 profile 已被修改，旧测量结果不得覆盖最新状态。

## 8. 预览图层

提供独立开关：

- 原始 Image；
- 中心线；
- 采样点；
- 共享 profile 锚点和圆形宽度手柄；
- 自动测量边界；
- 人工确认边界；
- 左右边界曲线；
- 生成的区域预览；
- 可见程度；
- 低可信度区域。

建议颜色约定：

- 绿色：人工确认；
- 黄色：自动测量但未确认；
- 红色：低可信度或异常；
- 蓝色：当前选中的锚点/手柄。

预览支持放大、缩小、平移和局部对比度增强。显示增强不得修改原始 Image 或 Annotation File。

边界预览必须明确是可视化结果，不得在未接受时隐式生成 polygon 或 mask Shape。

## 9. Annotation File 数据格式

在标准 LabelMe Shape 上增加可选的 `line_profile` 字段。该字段只对 `shape_type: "linestrip"` 有效。由于功能尚未发布，直接采用统一锚点格式，不保留旧的 `width_anchors` / `visibility_anchors` 结构，也不实现旧 profile 格式迁移。

```json
{
  "label": "LaserLine",
  "shape_type": "linestrip",
  "points": [[100, 80], [200, 90], [300, 110]],
  "line_profile": {
    "schema_version": 2,
    "path_mode": "continuous",
    "parameterization": "normalized_arc_length",
    "anchors": [
      {
        "position": 0.0,
        "width": {
          "value": 6.0,
          "source": "manual",
          "confidence": 1.0,
          "confirmed": true
        },
        "visibility": {
          "value": 1.0,
          "source": "manual",
          "confidence": 1.0,
          "confirmed": true
        }
      },
      {
        "position": 0.5,
        "width": {
          "value": 18.0,
          "source": "manual",
          "confidence": 1.0,
          "confirmed": true
        },
        "visibility": {
          "value": 0.45,
          "source": "auto",
          "confidence": 0.72,
          "confirmed": false
        }
      },
      {
        "position": 0.75,
        "width": null,
        "visibility": {
          "value": 0.12,
          "source": "manual",
          "confidence": 1.0,
          "confirmed": true
        }
      }
    ],
    "min_width": 4.0,
    "max_width": 40.0,
    "measurement_version": "line-profile-measurement-v3",
    "reviewed": false
  }
}
```

字段规则：

- `schema_version` 是 `line_profile` 数据结构版本，当前正式设计版本为 `2`；
- `path_mode` 只能为 `continuous` 或 `visible_only`；
- `parameterization` 首期固定为 `normalized_arc_length`；
- `anchors` 按 `position` 升序排列，位置范围为 `[0, 1]` 且不得重复；
- 每个锚点至少包含 `width` 或 `visibility` 其中一个属性；
- `width.value` 为完整宽度，单位为原始 Image 像素，必须为正数；
- `visibility.value` 范围为 `[0, 1]`；
- `source`、`confidence`、`confirmed` 必须分别保存在宽度或可见程度属性内；
- `source` 只能为 `auto` 或 `manual`；
- `confidence` 范围为 `[0, 1]`；
- `confirmed` 表示对应属性是否经过人工确认，不代表整个共享锚点已确认；
- `min_width`、`max_width` 是测量约束，不是实际统计结果；
- `measurement_version` 标识自动测量算法版本；
- `reviewed` 表示整条 Shape 是否完成审核。

### 9.1 读写与格式边界

- 没有 `line_profile` 的普通 `linestrip` 必须正常打开和编辑；
- 自动结果在接受前只能存在于内存临时状态，不得静默写入 Annotation File；
- 正式实现只读写统一的 `anchors` 格式，不再同时维护两套锚点数组；
- 未知 Shape 扩展字段不得导致 Annotation 无法打开；
- 已知但非法的 `line_profile` 必须给出结构化错误或警告，并避免覆盖原始有效文件；
- `line_profile` 应作为 Shape 级数据参与复制、撤销、重做、拆分和合并；
- 需要 profile-aware 导出或栅格化时，必须使用独立接口，不能改变现有其他 Shape 的行为；
- JSON 写入必须使用现有临时文件和原子替换机制；
- 保存失败、进程中断或非法 profile 不得破坏原 Annotation File；
- 不为未发布的旧 profile 格式实现读取、迁移、降级或双写逻辑。

### 9.2 长期架构边界

`line_profile` 不能作为未定义的 `other_data` 字典使用，正式实现必须提供类型化的数据边界：

- 增加 Qt-free 的 `LineProfile` 数据模型及其对应的 `LineProfileDict`/JSON 映射；
- `Shape` 和 `ShapeDict` 显式持有可选 profile，`other_data` 继续只负责未知扩展字段；
- profile 模型负责字段校验、默认值、归一化、插值和中心线变换；
- profile 模型不得依赖 QWidget、QPainter 或 Canvas 状态；
- Canvas 只负责手柄、预览和输入事件，不能自行解析 JSON 或决定 profile 版本；
- `_label_file` 只负责 Qt-free 的 JSON 编解码和错误包装，不承载交互逻辑；
- MainWindow 负责把 UI 操作转换为可撤销的 Shape/profile 修改，不重复实现 profile 算法；
- 自动测量器只接收 Image 像素、中心线和测量参数，输出不可变的测量结果，不直接修改 Shape；
- 接受测量结果时由单一的应用服务完成冲突检查、备份、合并和 dirty 标记。

数据模型必须把共享位置和属性值明确分层：

- `ProfileAnchor` 只负责 `position` 和两个可选属性；
- `ProfileValue` 负责单个属性的数值、来源、可信度和确认状态；
- 宽度与可见程度分别进行校验、插值、确认和冲突判断；
- 中心线几何变换只对 `ProfileAnchor.position` 执行一次，再由该锚点携带所有属性；
- UI 选择共享锚点后，通过活动属性切换编辑宽度或可见程度；
- 禁止通过两个数组的相同索引表示宽度与可见程度的对应关系。

推荐的核心操作接口为：

- `validate_profile(profile, shape, image_size)`；
- `evaluate_profile(profile, position)`；
- `remap_profile(profile, old_points, new_points)`；
- `reverse_profile(profile)`；
- `split_profile(profile, split_position)`；
- `merge_profiles(left, right, join_policy)`；
- `apply_measurement(profile, measurement, policy)`。

这些接口必须是确定性的、可单元测试的，并且不能依赖当前选中状态或界面控件。

### 9.3 版本与非法 profile 处理

- `schema_version` 只表示 profile 数据结构版本；`measurement_version` 只表示测量算法版本，两者不能混用；
- 当前版本直接使用统一 `anchors` 结构，不实现旧 profile 格式迁移；
- 未来删除或改变字段语义时必须提升 schema 版本，并在功能正式发布前确定新的读取策略；
- 没有 profile 的普通 Shape 不得在打开时自动生成并保存 profile；
- 发现非法 profile 时可以打开中心线，但 profile 必须标记为 invalid，禁用依赖它的编辑和导出操作，并显示可操作的错误信息；
- 未修改非法 profile 时保存应保留原始扩展数据；用户主动清除或替换 profile 后才允许写入新的合法 profile；
- 任何修复都必须经过临时文件和原子替换，失败时原 Annotation File 保持不变；
- 新版本发布前必须提供 schema 校验、非法数据处理和未知字段保留测试；不要求提供旧 profile 格式迁移测试。

## 10. 属性面板

选中 `linestrip` 后显示：

- Label；
- 中心线长度；
- 共享 profile 锚点数量；
- 含宽度属性的锚点数量；
- 含可见程度属性的锚点数量；
- 宽度实际最小值、最大值和平均值；
- 可见程度实际最小值和平均值；
- 自动测量状态；
- 低可信度位置数量；
- 当前 `path_mode`；
- 是否已审核。

共享 profile 锚点使用圆形手柄显示，但仍属于当前 `linestrip` 的 profile，不作为独立 Shape 选择或编辑。

支持：

- 自动测量宽度和可见程度；
- 重新测量选中区域；
- 接受全部自动结果；
- 仅接受高可信度结果；
- 恢复上一次人工结果；
- 清除宽度或可见程度 profile；
- 切换并明确确认路径语义；
- 标记为已审核。

清除 profile 后必须保留中心线，恢复为普通 `linestrip`。

### 10.1 选择与交互状态

- 同一时间最多有一个当前编辑的 `linestrip`、一个活动共享锚点和一个活动属性（宽度或可见程度）；
- 普通 Shape 选择继续使用现有 Canvas 和 Annotation List；
- 锚点选择是当前 Shape 内的临时视图状态，不写入 Annotation File；
- 圆形宽度手柄的命中半径按屏幕像素计算，保证不同缩放级别下可操作；
- 实际宽度由边界预览表示，手柄本身不能因宽度增大而遮挡整条中心线；
- 插入、删除、拖动和数值提交都必须产生一致的单次撤销记录；
- 失去 Shape 选择、切换 Image 或加载失败时，活动锚点必须清除。

## 11. 撤销、重做和修改边界

以下 Annotation 编辑必须进入现有 Shape 撤销栈：

- 新增、删除、移动中心线顶点；
- 新增、删除、移动共享 profile 锚点；
- 在共享锚点上新增、删除或修改宽度属性；
- 在共享锚点上新增、删除或修改可见程度属性；
- 修改宽度或可见程度；
- 接受自动测量结果；
- 批量应用测量结果；
- 修改 Shape 级测量参数；
- 切换 `path_mode`；
- 拆分、合并、反转和重新采样中心线。

自动测量本身不是 Annotation 修改；接受测量结果必须作为一次独立、可撤销的修改。自动测量结果不得覆盖人工确认值。

全局默认测量 Setting 按现有 Settings 机制处理，不强行纳入 Shape 撤销栈。若全局 Setting 会影响当前临时测量，必须重新测量并明确显示结果尚未接受。

## 12. 数据检查

### 12.1 Profile 检查

保存和接受测量结果前检查：

- `line_profile` 只能附加到 `linestrip`；
- `schema_version`、`path_mode` 和 `parameterization` 合法；
- 中心线至少包含两个点时才允许生成有效 profile；
- `position`、`width`、`visibility` 和 `confidence` 必须是有限数值；
- `position`、`visibility` 和 `confidence` 位于规定范围内；
- `width` 为正数，并满足宽度约束；
- 锚点按中心线位置有序；
- 不允许重复或无法排序的锚点；
- 插值结果不能产生零宽度、负宽度或未解释的尖峰；
- 自动测量失败的位置必须有低可信度或异常提示；
- `visible_only` 模式下不能用单个 Shape 隐式表达中间断线。

### 12.2 与现有几何兼容

- 不得把 profile 校验扩展为对所有历史 Shape 的全局严格几何校验；
- 现有越界点策略遵循 `canvas.allow_out_of_bounds_points`；
- 历史退化 Shape 可以继续读取；
- 新建或编辑 profile 时发现中心线无法支持 profile，应阻止该 profile 操作并保持原 Shape 不变；
- 检查结果分为错误、警告和提示：错误禁止本次 profile 保存，警告允许用户确认后保存，提示仅供参考。

## 13. 性能与并发

- 拖动锚点时边界预览目标延迟不超过 100ms；
- 单张 Image 自动测量不得阻塞 UI 线程；
- 批量测量必须支持后台运行、进度、取消和错误报告；
- 自动测量结果必须带有 Shape/Annotation 状态版本或等价标识；
- 中心线或 profile 修改后，旧异步结果必须被丢弃；
- 不重复创建大型临时 Image；
- 打开没有 profile 的 Annotation 时不能明显增加耗时；
- 保存失败不能造成原文件损坏。

## 14. 后续阶段：批量与连续图像/视频帧

本节不属于首期验收，但保留为后续工作。LabelMe 当前以目录中的 Image 和 Annotation File 表示连续帧，不额外假设存在内置视频对象。

### 14.1 批量测量

- 支持对目录中的 Annotation File 执行自动宽度/可见程度测量；
- 支持只处理没有 `line_profile` 的文件；
- 支持只处理未审核或低可信度文件；
- 支持输出测量报告；
- 后台运行，支持进度、取消和失败重试；
- 批量写入不得默认覆盖人工确认的锚点；
- 批量写入使用与单文件相同的原子保存规则。

### 14.2 连续图像/视频帧

- 支持复制上一帧的中心线和 profile；
- 支持根据当前 Image 重新自动测量；
- 支持显示上一帧与当前帧的中心线、宽度曲线和偏差；
- 支持批量确认连续帧中未发生变化的 profile；
- 单帧修改不得自动覆盖其他帧，除非用户明确选择批量应用；
- 必须明确帧顺序、目标 Image、路径映射和 profile 冲突处理规则。

## 15. 开发阶段

### 第一阶段：统一数据结构与格式

- 定义统一 `anchors` 结构、版本和校验规则；
- 定义共享 `position` 与独立 `width`/`visibility` 属性的类型边界；
- 明确属性缺失、属性单独删除和共享锚点删除的语义；
- 定义 `path_mode` 和归一化弧长规则；
- 将 profile 纳入 Shape/ShapeDict 的读写和复制路径；
- 只支持正式统一格式，不实现旧 profile 格式兼容或迁移；
- 增加统一格式、未知字段保留、非法 profile 和保存失败恢复测试。

### 第二阶段：中心线和 profile 编辑

- 增加共享锚点显示与编辑，以及活动属性切换；
- 实现分别基于属性集合的分段线性宽度和可见程度曲线；
- 实现插点、删点、移动、反转、拆分、合并和重新采样后的 profile 变换；
- 增加左右边界和低可信度预览；
- 接入现有 Shape 撤销栈和重做路径。

### 第三阶段：自动测量

- 增加法线方向采样；
- 增加自动宽度和可见程度测量；
- 增加可信度计算和异常识别；
- 增加后台任务、取消和过期结果丢弃；
- 增加自动结果接受、人工覆盖和局部重测。

### 第四阶段：批量与连续帧

- 增加目录批量测量和报告；
- 增加连续 Image 的上一帧复制与当前帧重测；
- 增加审核列表、统计和明确的批量应用操作。

### 第五阶段：稳定性与文档

- 完善异常输入处理；
- 完善大图和长序列性能；
- 完善自动保存和崩溃恢复；
- 完善用户操作文档、格式文档和 profile-aware 导出说明。

### 15.1 阶段门禁

每个阶段完成后才能进入下一阶段：

- **数据阶段门禁**：统一格式、未知字段、非法 profile 测试通过，profile 往返无字段丢失，保存失败可恢复；
- **编辑阶段门禁**：所有中心线变换均有 profile 映射测试，撤销/重做和自动保存测试通过，不能出现 profile 丢失；
- **测量阶段门禁**：合成图像基准、异常图像基准、异步取消和过期结果测试通过，人工确认值不会被覆盖；
- **批量阶段门禁**：任务可取消、失败可报告、原子写入有效、人工数据保护策略有测试；
- **发布阶段门禁**：性能预算、格式校验、文档、翻译、崩溃恢复和回滚方案完成评审。

### 15.2 测试矩阵

必须覆盖以下层次：

- **纯数据单元测试**：字段校验、默认值、属性缺失、分别插值、弧长映射、反转、拆分、合并和非法输入；
- **Codec 测试**：统一格式往返、未知字段保留、非法 profile 非破坏性处理和原子保存；
- **Canvas 单元测试**：锚点命中、拖动、缩放、边界绘制、端点/急转弯法线和可见性；
- **MainWindow 集成测试**：选择、属性面板、接受测量、dirty、自动保存、撤销/重做和失败恢复；
- **端到端 GUI 测试**：新建、编辑、复制、拆分、合并、重开和不同缩放级别下的交互；
- **测量基准测试**：已知宽度、宽度突变、低对比度、散斑、过曝、不对称和图像边缘；
- **回归测试**：其他 Shape 类型、现有 linestrip、现有圆形、普通导出和没有 profile 的 Annotation File 行为不变。

### 15.3 发布与维护

- 首期作为可控的 Preview Setting 或明确的实验能力发布，稳定一个完整版本周期后再评估毕业；
- profile schema 变更必须记录新格式定义和生效范围；
- measurement algorithm 变更必须更新 `measurement_version`，并保留旧结果的可解释性；
- 破坏性数据变更必须提供备份和回滚方案；
- 性能、错误和取消必须写入可诊断日志，但不得记录完整 Image 内容；
- 用户文档必须说明 profile 只由本版本支持；
- 每个发布版本必须保留一组固定的示例 Annotation File 作为格式回归样本。

## 16. 验收标准

### 16.1 标注效率

- 标注员只绘制一条连续中心线即可开始工作；
- 一条线通常通过 3～8 个共享 profile 锚点完成修正；
- 不要求手工绘制完整左右边界；
- 自动测量异常位置可以被快速定位和修正。

### 16.2 标注正确性

- 能表达同一条线上 5～6px 到 20～30px 的宽度变化；
- 连续中心线模式下暗区不会被自动错误截断；
- visible-only 模式下中间断线不会被错误连接；
- 散斑不会直接形成大量宽度尖峰；
- 人工确认后的宽度和可见程度不会被再次自动测量覆盖；
- 反转、拆分、合并和重新采样后 profile 仍与中心线物理位置一致；
- 保存后重新打开，中心线、profile、来源、可信度和审核状态完全一致。

### 16.3 文件完整性

- 没有 `line_profile` 的普通 Annotation File 可打开、编辑和保存；
- 缺少 `line_profile` 时功能降级但不报错；
- 未知扩展字段不会破坏其他 LabelMe 功能；
- 非法 profile 有明确错误/警告，不会覆盖原有效文件；
- JSON 损坏或写入中断时原文件可恢复；
- 统一 `anchors` 格式保存后，宽度和可见程度属性均可完整恢复；
- 不要求读取未发布的旧 profile 格式。

### 16.4 交互与性能

- 控制点、边界和低可信度状态清晰可见；
- 自动、人工和低可信度结果有明显区别；
- 所有 Annotation 修改支持撤销和重做；
- 单张 Image 自动测量不阻塞 UI；
- 异步旧结果不会覆盖新编辑；
- 后续批量操作具备进度、取消和结果报告。

## 17. 不在本需求范围内

- 不修改其他标注类型的现有行为；
- 不新增替代 `linestrip` 的 Shape 类型；
- 不把共享 profile 锚点保存为普通 `circle` 或 `point` Shape；
- 不要求标注员逐像素绘制条纹边界；
- 不把 Image 亮度直接当作线宽；
- 不自动删除标注员在 `continuous` 模式下明确绘制的中心线；
- 不在普通保存时将中心线强制转换为固定宽度 polygon 或 mask；
- 不在首期实现完整目录批量处理、视频对象管理或大规模审核系统；
- 不改变现有 LabelMe 全局越界点和历史退化几何的兼容策略。

## 18. 实现 Checklist

以下清单按依赖关系排列。后续阶段不得绕过前置阶段的验收项；每个复选项只有在代码、测试和必要文档都完成后才能勾选。

### 18.1 阶段 0：冻结决策与基线

- [x] 确认 `line_profile` 只附加到 `shape_type="linestrip"`；
- [x] 确认 `path_mode` 的正式取值为 `continuous` 和 `visible_only`；
- [x] 确认首期使用 `normalized_arc_length`，不保存中心线顶点编号；
- [x] 确认宽度字段表示完整直径，圆形手柄内部使用 `width / 2`；
- [x] 确认一个共享锚点只保存一个 `position`；宽度和可见程度分别保存数值、`source`、`confidence` 和 `confirmed`；
- [x] 确认宽度或可见程度属性可以单独缺失，但共享锚点至少存在一个属性；
- [x] 确认删除单个属性不会误删另一属性，删除空锚点才删除共享位置；
- [x] 确认 `min_width`/`max_width` 是测量约束，不是观测统计值；
- [x] 确认非法 profile 的非破坏性读取、保存和修复策略；
- [x] 确认 `schema_version` 与 `measurement_version` 的升级规则；
- [x] 记录现有单元测试、端到端测试、保存格式测试和性能基线；
- [x] 为新增数据格式建立至少一份固定的示例 Annotation File；
- [x] 将本阶段决策写入相关 ADR 或格式文档，避免后续实现重新解释字段含义。

### 18.2 阶段 1：Qt-free 数据模型

- [x] 新增独立的 Qt-free profile 模块，建议命名为 `labelme/_line_profile.py`；
- [x] 定义 `LineProfile`、`ProfileAnchor` 和 `ProfileValue` 数据类型；
- [ ] 删除独立的宽度锚点/可见程度锚点类型及其并行数组；
- [x] 明确可变编辑对象与序列化快照之间的边界；
- [x] 为共享锚点提供确定性的排序、复制和深拷贝行为；
- [x] 实现 `validate_profile`，覆盖共享位置、属性缺失、属性范围、有限数值、重复位置和模式约束；
- [x] 实现按属性集合分别插值的 `evaluate_width` 和 `evaluate_visibility`；
- [x] 明确端点缺少锚点、单锚点和空 profile 的行为；
- [x] 为宽度和可见程度属性分别定义 `source`、`confidence`、`confirmed` 不变量；
- [x] 将可选 profile 纳入 `Shape` 和 `ShapeDict` 的显式字段；
- [x] 保持 `other_data` 只存放未识别的扩展字段；
- [x] 确认 `Shape.copy()` 能完整复制 profile，且不会共享可变锚点列表；
- [x] 为数据模型编写纯 Python 单元测试，不依赖 QApplication。

### 18.3 阶段 2：Annotation File 编解码与统一格式

- [x] 在 Shape JSON 解析中识别统一 `line_profile`；
- [x] 对没有 `line_profile` 的旧 Shape 返回 `None` 或等价的空状态；
- [x] 对统一 `anchors` profile 完成 JSON → 模型 → JSON 往返测试；
- [x] 对未知 Shape 字段继续执行原有保留逻辑；
- [x] 对统一 `line_profile` 的非法字段生成可定位到 Shape/字段的错误信息；
- [x] 实现非法 profile 的非破坏性读取状态，不让整个 Annotation 丢失；
- [x] 保存未修改的非法 profile 时保留原始扩展数据；
- [x] 用户清除或替换非法 profile 后才写入新的合法数据；
- [x] 实现 `schema_version=2` 的严格格式校验；
- [x] 明确未来 schema 版本的不可编辑错误提示；
- [x] 验证 profile 不能附加到非-linestrip Shape；
- [x] 验证 JSON `null`、整数、浮点、布尔值和字符串错误类型；
- [x] 验证临时文件、原子替换、权限保留和写入失败恢复；
- [x] 增加无 profile、统一格式、未知字段、非法 profile 和中断保存测试；

### 18.4 阶段 3：中心线弧长与 profile 变换

- [x] 实现中心线累计弧长计算，并处理重复点和零长度线段；
- [x] 实现归一化位置与中心线坐标之间的双向映射；
- [x] 明确端点、急转弯和零长度段的切线/法线策略；
- [x] 实现 `remap_profile(old_points, new_points)`；
- [x] 实现整体平移后 profile 保持不变；
- [x] 实现中心线顶点移动后的锚点物理位置重映射；
- [x] 实现在线段上插入顶点且不改变锚点物理位置；
- [x] 实现删除顶点后的锚点保留、裁剪或用户提示；
- [x] 实现中心线反转时 `position -> 1 - position`；
- [x] 实现截断时 profile 的区间裁剪与重新归一化；
- [x] 实现延长时 profile 的端点扩展规则；
- [x] 实现拆分时左右 profile 分配、端点补齐和重新归一化；
- [x] 实现合并时方向判断、profile 拼接和重叠区冲突策略；
- [x] 实现重新采样后 profile 数值和物理位置保持一致；
- [x] 任一映射失败时回滚到原 Shape，不产生半完成状态；
- [x] 为每种变换增加正向、边界、重复点、空 profile 和失败回滚测试。
- [ ] 确保所有中心线变换只遍历一次共享锚点，并同时携带两个可选属性；
- [ ] 增加宽度属性缺失、可见程度属性缺失和两者采样位置不同的变换测试。

### 18.5 阶段 4：现有 Canvas/Shape 操作接入

- [x] 在现有 linestrip 顶点拖动提交点接入 profile 重映射；
- [x] 在现有“添加点到边”操作接入 profile 重映射；
- [x] 在现有“删除点”操作接入 profile 重映射；
- [x] 在现有拆分操作中复制并切分 profile，不丢失来源和审核状态；
- [x] 在现有合并操作中拼接 profile，并处理反向 Shape；
- [x] 确保整条 Shape 移动只改变中心线坐标，不改变 profile 数值；
- [x] 确保 Shape 复制、剪贴板复制和跨文件复制包含 profile；
- [x] 为普通没有 profile 的 linestrip 保持原有行为；
- [x] 在每次 profile 变更前后正确调用现有 Shape backup/restore；
- [x] 确保一次用户操作只产生一个撤销记录；
- [x] 确保 undo/redo 后 Annotation List、Canvas 和 profile 状态一致；
- [x] 修复现有拆分/合并中新建 Shape 丢失扩展数据的问题；
- [x] 增加现有 linestrip 回归测试，证明其他 Shape 类型不受影响。

### 18.6 阶段 5：圆形宽度手柄与属性面板

- [x] 提取可复用的距离/半径输入控件能力，避免把 `CircleRadiusWidget` 直接绑定到 linestrip；
- [x] 新增 profile 宽度控件，界面字段显示“宽度”而不是“半径”；
- [x] 在 Canvas 中绘制屏幕尺寸稳定的圆形宽度手柄；
- [x] 根据中心线归一化位置计算手柄中心；
- [x] 根据宽度计算可视化直径，但限制手柄显示尺寸避免遮挡；
- [x] 实现手柄命中检测，命中半径按屏幕像素而不是图像像素计算；
- [x] 支持选中、插入、删除和拖动共享 profile 锚点；
- [x] 支持拖动手柄中心修改共享 `position`，同步移动其所有属性；
- [x] 支持拖动手柄边缘修改当前锚点的 `width` 属性；
- [x] 支持属性面板分别编辑宽度和可见程度的数值、来源、可信度和确认状态；
- [x] 支持在同一共享锚点上切换活动属性，不创建第二套可见程度锚点；
- [x] 支持只含宽度或只含可见程度的共享锚点；
- [x] 活动锚点在切换 Shape、Image、模式和加载失败时清除；
- [x] 圆形宽度手柄不进入 Annotation List，不创建普通 circle/point Shape；
- [x] 接入现有缩放、平移、主题颜色和状态栏提示；
- [ ] 为不同缩放级别、边缘位置、密集锚点和大宽度增加 GUI 测试；
- [ ] 增加宽度/可见程度不同位置采样、属性单独删除和共享锚点删除 GUI 测试。

### 18.7 阶段 6：边界、曲线与预览

- [x] 实现宽度曲线采样，首期使用确定性的分段线性插值；
- [x] 实现可见程度曲线采样；
- [x] 将两种曲线采样改为分别从共享锚点中筛选对应属性，不依赖锚点索引配对；
- [x] 实现中心线局部法线计算；
- [x] 实现左右边界曲线生成；
- [x] 明确急转弯处的连接方式，避免边界自交或异常尖角；
- [x] 明确端点边界封口方式；
- [x] 为自动、人工确认、低可信度和当前选中状态提供独立视觉样式；
- [x] 增加预览图层开关，不改变原始 Image；
- [x] 生成区域预览时只作为绘制结果，不自动创建 polygon 或 mask Shape；
- [x] 避免复用固定 `line_width` 的普通 linestrip 栅格化作为 profile 预览；
- [x] 验证边界预览不影响普通 Shape 的绘制和命中检测；
- [x] 为合成中心线、急转弯、突变宽度、越界和低可信度数据增加渲染测试。

### 18.8 阶段 7：Settings、撤销和保存流程

- [x] 在 Settings Schema 中加入全局测量默认值；
- [x] 明确每个全局 Setting 的单位、范围、默认值和重启要求；
- [x] 支持 Shape 级测量参数覆盖；
- [x] 明确全局 Setting 修改不进入 Shape 撤销栈；
- [x] 明确 Shape 覆盖值修改进入 Shape 撤销栈；
- [x] 接受全部自动结果作为一次独立可撤销操作；
- [x] 仅接受高可信度结果作为一次独立可撤销操作；
- [x] 恢复人工结果不得覆盖当前未接受的其他人工修改；
- [x] 清除 profile 保留中心线，并可撤销恢复；
- [x] profile 修改触发现有 dirty 和 auto-save；
- [x] 自动保存失败时保留内存中的最后有效状态和错误提示；
- [x] 切换 Image 或关闭窗口时复用现有保存确认流程；
- [x] 验证自动保存不会把临时自动测量结果静默写入 Annotation File；
- [x] 增加保存、取消保存、写入失败、切换 Image 和关闭窗口测试。

### 18.9 阶段 8：自动测量核心

- [x] 定义测量输入、输出和不可变结果类型；
- [x] 定义 `measurement_version` 的生成和升级规则；
- [x] 实现沿中心线按间距采样；
- [x] 实现局部法线测量线；
- [x] 实现背景估计和局部亮度/对比度分析；
- [x] 实现候选边界检测；
- [x] 实现连续亮区、对称性和边界可靠性评分；
- [x] 实现宽度候选、可见程度候选和 `confidence` 输出；
- [ ] 让一次测量采样输出一个共享锚点结果，其中宽度和可见程度属性可分别为空；
- [x] 确保接受测量结果时按 `position` 合并属性，不通过宽度/可见程度数组索引配对；
- [x] 识别过曝、低对比度、散斑、左右不对称、越界和无边界；
- [x] 为异常位置提供邻近可靠位置插值推荐值；
- [x] 自动结果默认标记为 `source="auto"`、`confirmed=false`；
- [x] 自动测量器不能直接修改 Shape 或写文件；
- [x] 为固定合成图像建立测量基准和允许误差；
- [x] 为算法异常、空图像、极短线、重复点和超大图增加测试。

### 18.10 阶段 9：后台任务与结果提交

- [x] 为测量任务建立独立 `QThread`/Worker，不复用模型下载业务对象；
- [x] Worker 不直接访问 QWidget、Canvas 或 MainWindow；
- [x] 为每次任务生成任务 ID 和 Shape/Annotation 状态版本；
- [x] 支持开始、进度、完成、失败和取消信号；
- [x] 支持用户在任务期间继续浏览界面；
- [x] 任务完成后校验任务 ID、Image、Shape 和状态版本；
- [x] 过期结果只提示用户，不得覆盖当前 profile；
- [x] 接受结果前再次执行 profile 校验和人工修改冲突检查；
- [x] 接受操作由单一应用服务完成备份、合并、dirty 和刷新 UI；
- [x] 关闭窗口、切换 Image 或重测时安全取消或隔离旧任务；
- [x] 增加后台取消、失败、过期结果和窗口关闭测试。

### 18.11 阶段 10：批量与连续帧

- [x] 明确批量任务的输入目录、文件筛选和输出策略；
- [x] 支持只处理缺少 profile 的 Annotation File；
- [x] 支持只处理未审核或低可信度文件；
- [x] 支持 dry-run、结果报告和失败清单；
- [x] 支持批量任务断点；
- [x] 支持批量任务取消和失败重试；
- [x] 批量写入沿用单文件原子保存和人工数据保护策略；
- [x] 明确连续 Image 的帧排序、上一帧来源和目标映射；
- [x] 复制上一帧前检查 Shape 数量、Label、路径和 profile 完整性；
- [x] 显示当前帧与上一帧的中心线、宽度和可见程度差异；
- [x] 批量应用前提供预览、确认和回滚信息；
- [x] 单帧修改不得隐式写入其他帧；
- [x] 为批量和连续帧建立独立端到端测试，不阻塞首期发布。

### 18.12 阶段 11：文档、发布与维护

- [x] 编写用户文档：绘制、测量、确认、修正、审核和清除 profile；
- [x] 编写 Annotation File profile 格式文档和示例；
- [x] 编写统一 `anchors` profile 格式说明；
- [x] 编写测量算法版本和结果解释说明；
- [x] 为新增 UI 字符串补齐翻译提取和翻译文件；
- [x] 更新 CHANGELOG，说明统一 `anchors` 字段和发布前格式变更；
- [x] 提供固定示例文件作为每次发布的格式回归样本；
- [x] 记录性能基线、已知限制和推荐硬件；
- [x] 定义错误日志字段，禁止记录完整 Image 或敏感路径内容；
- [x] 定义 schema 校验失败和发布回滚流程；
- [ ] 完成阶段门禁评审后再开放下一阶段功能；
- [x] 发布后收集 profile 读取失败、测量失败、任务取消和保存失败指标。

## 19. Definition of Done

首期功能只有同时满足以下条件才算完成：

- [x] 统一锚点数据模型、JSON 编解码和非法数据策略已确定并测试通过；
- [ ] 没有 profile 的 Annotation File、未知字段和普通 linestrip 回归测试全部通过；
- [x] 中心线所有已支持编辑操作都不会丢失或错误重定位 profile；
- [x] 圆形宽度手柄可在不同缩放级别下稳定选择、拖动和输入；
- [x] 宽度、可见程度、来源、可信度和审核状态可往返保存；
- [x] 自动测量不会覆盖人工确认结果，过期异步结果不会污染当前 Shape；
- [x] profile 修改、接受、清除和中心线变换都支持撤销/重做；
- [x] 自动保存、保存失败、切换 Image 和关闭窗口均保持原有数据安全保证；
- [x] 性能满足预览 100ms 目标，自动测量不阻塞 UI；
- [x] 用户文档、格式文档、CHANGELOG、翻译和发布说明完成；
- [ ] 通过阶段门禁和发布评审后，才从 Preview 能力进入稳定支持。
